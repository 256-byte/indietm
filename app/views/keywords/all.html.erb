<div class="relative items-start justify-between rounded-xl border border-gray-100 p-4 shadow-xl sm:p-6 lg:p-8 mx-auto w-[70%] bg-white mt-4">
  <table class="table-responsive">
    <thead>
      <tr>
        <th></th>
        <th></th>
        <th>Keyword</th>
      </tr>
    </thead>
    <tbody>
      <% @keywords.each do |keyword| %>
        <tr>
          <td><%= link_to "Edit", edit_keyword_path(keyword.id)%></td>
          <td><%= link_to "Delete", keyword_path(keyword.id), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete the translation?" } %></td>
          <td><%= keyword.word%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
